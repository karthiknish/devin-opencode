{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "opencode-devin-plugin",
  "version": "0.1.0",
  "description": "OpenCode v2 plugin to connect your Devin account and drive cloud Devin sessions from the OpenCode agent.",
  "type": "module",
  "license": "MIT",
  "keywords": ["opencode", "opencode-plugin", "devin", "cognition", "ai"],
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "files": ["src"],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@opencode-ai/plugin": "next"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.2"
  }
}
